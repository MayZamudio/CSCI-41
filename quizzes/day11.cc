#include <iostream>
#include <cmath>
#include <algorithm>
#include <vector>
using namespace std;

class Point {
	int x = 0, y = 0;
	public:
	void set_x(int new_x) { x = new_x; }
	void set_y(int new_y) { y = new_y; }
	int get_x() { return x; }
	int get_y() { return y; }
};

int main() {
}
