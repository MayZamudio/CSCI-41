#include <iostream>
#include <vector>

using namespace std;

template <class T>

class HashTable {
	//Code will go there
	public:
		void insert(T new_t) {
		
		}
		bool search(T other_t) {
			return true;
		}
		void delete (T other_t) {
		
		}
};

int main () {
	HashTable<int> table;
	while(true) {
		cout << "Do you want to 1.) Insert 2.) Search 3.) Delete\n" << endl;
		int choice = 0;;
		cin >> choice;
		cout << "What int?\n";
		int x = 0;

	}
}
